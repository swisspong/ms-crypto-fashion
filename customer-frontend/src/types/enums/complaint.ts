
export enum TypeFormat {
    PRODUCT= "product",
    MERCHANT= "merchant"
}

export enum StatusFormat {
    PENDING= "pending",
    PROGRESS= "progress",
    RESOLVED= "resolved",
    CLOSING= "closing"
}